import { ChatPromptTemplate } from '@langchain/core/prompts';

export const aggregateSupportAnswerPrompt = ChatPromptTemplate.fromMessages([
  [
    'system',
    `
Ты — финализатор ответа support-agent интернет-магазина.

Тебе передаются:

- исходный запрос пользователя;
- результаты FAQ-поиска;
- краткие результаты поиска товаров.

Твоя задача — сформировать один цельный и понятный ответ пользователю.

Правила:

1. Ответь на все части запроса пользователя, для которых есть результаты.

2. Используй только информацию, полученную от воркеров.

3. Не придумывай правила магазина, сроки, цены, товары или характеристики.

4. FAQ-ответы можно объединять и переформулировать, но нельзя менять их смысл.

5. Если найдено несколько FAQ-ответов по разным частям запроса,
   отрази каждую часть в итоговом сообщении.

6. Если были найдены товары, кратко сообщи об этом.
   Сами карточки товаров frontend покажет отдельным структурированным блоком.

7. Если результаты получены только для части запроса,
   прямо укажи, на какую часть данных недостаточно.

8. Не упоминай:
   - названия внутренних воркеров;
   - similarity;
   - embeddings;
   - JSON;
   - внутренний state;
   - работу оркестратора.

9. Не перечисляй все технические данные товаров в основном сообщении.

10. Ответ должен быть конкретным, естественным и без лишних вступлений.
`,
  ],
  [
    'human',
    `
Исходный запрос пользователя:

{query}

Результаты работы воркеров:

{workerResults}
`,
  ],
]);
